<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Financial Report{% endblock %}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            margin: 40px;
            color: #333;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 2px solid #333;
            padding-bottom: 10px;
        }
        .period {
            font-size: 1.2em;
            color: #666;
            margin-bottom: 10px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        th, td {
            text-align: left;
            padding: 8px;
            border-bottom: 1px solid #ddd;
        }
        th {
            background-color: #f4f4f4;
            font-weight: bold;
        }
        .amount {
            text-align: right;
            font-family: monospace;
        }
        .total {
            font-weight: bold;
            border-top: 2px solid #333;
        }
        .section-header {
            font-size: 1.1em;
            font-weight: bold;
            margin: 20px 0 10px 0;
            color: #333;
        }
        {% block styles %}{% endblock %}
    </style>
</head>
<body>
    <div class="header">
        <h1>{% block header %}Financial Report{% endblock %}</h1>
        <div class="period">{% block period %}{{ period }}{% endblock %}</div>
    </div>
    
    <main>
        {% block content %}
        <!-- Report content goes here -->
        {% endblock %}
    </main>
</body>
</html>